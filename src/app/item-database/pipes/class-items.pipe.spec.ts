import { ClassItemsPipe } from './class-items.pipe';

describe('ClassItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
